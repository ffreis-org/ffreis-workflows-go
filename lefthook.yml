remotes:
  - git_url: https://github.com/FelipeFuhr/ffreis-platform-standards
    ref: v1.3.0
    configs:
      - lefthook/base.yml
      - lefthook/actionlint.yml

# fmt-check targets examples/ only — repo-local override
pre-commit:
  commands:
    fmt-check:
      glob: "examples/**/*.go"
      run: make fmt-check

pre-push:
  parallel: false
  commands:
    lint:
      run: make lint
    actionlint:
      run: actionlint
